// Put your Google Maps API key here if you want the app to use
// Google Places (Find Place from Text) for geocoding when
// expo Location.geocodeAsync is unavailable (SDK changes).
// IMPORTANT: Do NOT commit a real API key to public repos.
export const GOOGLE_API_KEY = "YOUR_GOOGLE_API_KEY";

export default {
  GOOGLE_API_KEY,
};
